<% @products.each do |product| %>
  <p>Title: <%= product.title %></p>
  <p>Description: <%= product.description %></p>
  <p>Price: <%= product.price %></p>
  <p>Category: <%= product.category.title %></p>
  <%= link_to 'Add one', products_path(product), class: 'btn btn-info' %>
<% end %>
